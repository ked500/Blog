<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: {method: :delete},
             remote: true) do |f| %>
  <%= button_tag type: :submit, class: "btn btn-white follow-button" do %>
    <i class="fa-solid fa-heart-crack fa-xl" style="color:red"></i>
  <% end %>
<% end %>